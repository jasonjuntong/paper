Find the most recent file in `.claude/session/summaries/` — filenames are formatted YYYY-MM-DD_HH-MM.md, so sort descending and take the first. Read it, then give a one-paragraph recap of what was last worked on and what's next so we can continue.

If the directory or files don't exist, say: "No session summaries found at `.claude/session/summaries/` — the path may have changed. Update `resume-session.md` with the correct path."
